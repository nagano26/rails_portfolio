<header class="bg-success py-2">
  <div class="container px-4 px-lg-5 my-5">
      <div class="text-white">
          <h2 class="display-4 fw-bolder">あなたの移住体験談</h2>
      </div>
  </div>
</header>

<%= render "shared/error", obj: @work %>

<section class="section mb-5">
  <div class="container mt-5">
  <br>
    <h1>あなたの体験を教えて下さい</h1>
  <br>    
    <div class="row">
      <div class="col-10">
          <%= form_for @work do |f| %>
              <div class="form-group">
                <%= f.label :before_work, "・移住前のお仕事は？", class: "label mt-4" %>
                <%= f.select :before_work, Ride.work_selects.keys, {prompt: "選択してください"}, class: "form-control "%>
              </div>

              <div class="form-group">
                <%= f.label :current_work, "・移住後のお仕事は？", class: "label mt-4" %>
                <%= f.select :current_work, Ride.work_selects.keys, {prompt: "選択してください"}, class: "form-control" %>
              </div>
              
              <div class="form-group">
                <%= f.label :others_work  , "・移住先では、皆さんどんなお仕事をされていますか？", class: "label mt-4" %>
                <%= f.text_area :others_work, :rows => "5", class: "form-control" ,placeholder:"私の移住先は、かなりの田舎の為、農家が多い印象です。" %>
              </div>

              <div class="form-group">
                <%= f.label :message_work, "・仕事について", class: "label mt-4" %>
                <%= f.text_area :message_work, :rows => "5", class: "form-control", placeholder: "田舎なので現地で仕事を探すとなれば、選択肢が狭くなります。ただ、地域全体で移住支援に力を入れており、市役所などの斡旋で仕事は見つかると思います。一度、役所などに相談してみて下さい。" %>
              </div>

              <%= f.submit '投稿', class: "btn btn-outline-primary mt-4 mr-5" %>
              <%= link_to '前のページに戻る', subhome_index_path ,class: "btn btn-primary mt-4"%>
          <% end %>
      </div>
    </div>
  </div>
</section>